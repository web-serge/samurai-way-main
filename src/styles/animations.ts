import {keyframes} from 'styled-components';

export const jump = keyframes `
  0% {transform: translate(-50%, 0px)}
  50% {transform: translate(-50%, -.5rem) scale(-1, 1)}
  0% {transform: translate(-50%, 0px)}
`